services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5050:5050
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 5050
    restart: unless-stopped